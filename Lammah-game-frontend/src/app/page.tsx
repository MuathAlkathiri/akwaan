import { WorldsHome } from "@/features/worlds";

/**
 * Worlds are the product, so the home page is the World browser for everyone.
 * The admin control panel lives at /admin.
 */
export default function Home() {
  return <WorldsHome />;
}
